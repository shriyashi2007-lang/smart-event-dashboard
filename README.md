# Smart Event Dashboard

## 📖 Overview

The **Smart Event Dashboard** is a dynamic web application built using **Vanilla JavaScript**.  
It demonstrates core concepts of **DOM manipulation**, **event handling**, and **interactive UI updates** without using any external libraries or frameworks.

Users can create, view, interact with, and manage event cards dynamically on the webpage.

---

## 🎯 Learning Objectives

- Understand and apply DOM traversal and manipulation
- Dynamically create, update, and remove HTML elements
- Handle user interactions using JavaScript events
- Implement event delegation and event bubbling
- Compare `innerText`, `innerHTML`, and `textContent`
- Debug DOM-related issues using browser DevTools

---

## 🛠️ Technology Stack

- **HTML5** – Page structure  
- **CSS3** – Basic styling (no frameworks)  
- **Vanilla JavaScript** – DOM manipulation & event handling  

---

## ⚠️ Restrictions

- ❌ No UI libraries (Bootstrap, Tailwind, etc.)
- ✅ Only plain HTML, CSS, and JavaScript allowed

---

## ✅ Features

- Event creation form
- Dynamically generated event cards
- DOM traversal and selection
- Multiple event handlers (click, hover, etc.)
- Event delegation
- Text and style manipulation using JavaScript

---

## 📂 Project Structure

